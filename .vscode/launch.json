{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: Cloud Segmentation",
            "type": "debugpy",
            "request": "launch",
            "program": "./CloudSegmentation/cloud_segmentation.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${cwd}"
            }
        }
    ]
}